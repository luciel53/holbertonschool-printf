#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

int _printf(const char *format, ...);
int _putchar(char c);



/**
 * typedef structure - it is the struction
 * @C: ......
 */

typedef struct format
{
	char c;
	char s;
} format;

int main(void)
{
	struct format;
	return (0);
}

#endif
