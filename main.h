#ifndef MAINN_H
#define MAIN_H
#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

int _printf(const char *format, ...);
int _putchar(char c);



/**
 * structure
 *
 */

typedef struct format;
{
	char c;
	char s;

};

int main(void)
{
	struct format;
	return (0);
}

#endif
